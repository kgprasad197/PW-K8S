kind: Service     
apiVersion: v1
metadata:
  name: spring-service
spec:
  selector:
    app: spring   
  ports:         
  - name: http   
    protocol: TCP
    port: 8080
    targetPort: 80
  type: LoadBalancer
